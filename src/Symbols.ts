export class ValienteSymbol {
  static readonly OPTIONS = Symbol("options");
  static readonly VALIDATORS = Symbol("validators");
  static readonly DATA = Symbol("data");
  static readonly TYPE = Symbol("type");
}
